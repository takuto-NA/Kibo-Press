# Create Document Config 実行前の例

このファイルは **front matter が無い**状態の例です。

VS Code でこのファイルを開き、先頭行が空であることを確認してから **Kibo-Press: Create Document Config** を実行すると、[document-config-template.md](document-config-template.md) と同じ内容が先頭に挿入されます。

先頭行が空でない場合は、拡張が警告を出します。
